<UserControl x:Name="ATMR_TimerTile"
    x:Class="ApplianceTMR.TimerTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ApplianceTMR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="160"
    d:DesignWidth="480">
    
    <Grid Background="DarkBlue" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColumnIcon" Width="160" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image Name="ApplIcon" Grid.Row="0" Grid.Column="0" Margin="8" Source="/Assets/Washing-Machine.png" />
        <Grid Name="Details"  Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>

            <AppBarButton Name="abTimeDown" Grid.Row="0" Grid.Column="0" Icon="Back" IsCompact="True" VerticalAlignment="Center"  Click="abTimeDown_Click"/>
            <TextBlock Name="ApplTime" Grid.Row="0" Grid.Column="1" Text="0" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="88"/>
            <AppBarButton Name="abTimeUp" Grid.Row="0" Grid.Column="2" Icon="Forward" IsCompact="True" VerticalAlignment="Center" Click="abTimeUp_Click" />
            <TextBlock Name="ApplStartTime" Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="3" Text="Press To Start" TextAlignment="Center" FontSize="30"/>
        </Grid>        
    </Grid>
</UserControl>
